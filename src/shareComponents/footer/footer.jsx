import React from 'react';
import s from "./footer.module.scss"

const Footer = () => {
    return (
        <div className={s.footer}>
            Task work for Headworkers
        </div>
    );
};

export default Footer;